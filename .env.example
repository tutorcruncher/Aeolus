# Server Configuration
PORT=3000

# Redis Configuration
# For Heroku: Use the REDIS_URL provided by Heroku Redis addon
# For local development: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# CORS Configuration
# Set to your TC2 domain in production (e.g., https://tutorcruncher.com)
# Use * for development (not recommended for production)
CORS_ORIGIN=*

# Authentication Configuration
# This prefix should match what TC2 uses when storing auth tokens in Redis
# TC2 will store tokens like: tc2:socket:auth:{token_value}
# Token value should contain: {"userId": 123, "chatSessionId": "chat_123"}
AUTH_TOKEN_PREFIX=tc2:socket:auth
